% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/passphrase.R
\name{age_passphrase}
\alias{age_passphrase}
\alias{age_encrypt_raw_passphrase}
\alias{age_decrypt_raw_passphrase}
\alias{age_encrypt_file_passphrase}
\alias{age_decrypt_file_passphrase}
\title{Encrypt and decrypt with a passphrase}
\usage{
age_encrypt_raw_passphrase(x, passphrase = NULL, armor = FALSE, log_n = 18)

age_decrypt_raw_passphrase(x, passphrase = NULL)

age_encrypt_file_passphrase(
  input,
  output = NULL,
  passphrase = NULL,
  armor = FALSE,
  overwrite = FALSE,
  log_n = 18
)

age_decrypt_file_passphrase(
  input,
  output = NULL,
  passphrase = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{x}{For \code{age_encrypt_raw_passphrase()}, a raw vector of plaintext. For
\code{age_decrypt_raw_passphrase()}, a raw vector of ciphertext or a length-1
armored string.}

\item{passphrase}{A length-1 character string. If \code{NULL} (the default) and
the session is interactive, it is prompted for securely via
\code{askpass::askpass()} (the suggested \pkg{askpass} package must be
installed). Avoid hard-coding passphrases in scripts.}

\item{armor}{If \code{TRUE}, produce ASCII-armored output.}

\item{log_n}{scrypt work factor, as the base-2 logarithm of the parameter
N. Higher is slower and more brute-force resistant. Defaults to 18;
values above 22 are rejected (also on decrypt) to bound work.}

\item{input, output}{File paths. \code{output = NULL} appends/strips \code{.age} as in
\code{\link[=age_encrypt_file]{age_encrypt_file()}}.}

\item{overwrite}{If \code{FALSE} (default), error when \code{output} already exists.}
}
\value{
The raw functions return a raw vector; the file functions return the
output path invisibly.
}
\description{
Passphrase-based (scrypt) encryption, a separate mode from the
recipient-based \code{\link[=age_encrypt_raw]{age_encrypt_raw()}} family. A file encrypted this way is
decrypted with the matching \verb{age_decrypt_*_passphrase()} function and the
same passphrase — no key pair is involved.
}
\examples{
ct <- age_encrypt_raw_passphrase(
  charToRaw("secret"),
  passphrase = "hunter2",
  log_n = 8
)
rawToChar(age_decrypt_raw_passphrase(ct, passphrase = "hunter2"))
}
