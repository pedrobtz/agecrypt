% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text.R
\name{age_text}
\alias{age_text}
\alias{age_encrypt_text}
\alias{age_decrypt_text}
\title{Encrypt and decrypt a single string}
\usage{
age_encrypt_text(x, recipients, armor = TRUE)

age_decrypt_text(x, identities)
}
\arguments{
\item{x}{For \code{age_encrypt_text()}, a length-1 character string (errors on
longer input rather than silently collapsing lines). For
\code{age_decrypt_text()}, ciphertext as a raw vector or armored string.}

\item{recipients}{Character vector of \code{"age1..."} recipient strings.}

\item{armor}{Must be \code{TRUE} (the default): \code{age_encrypt_text()} always
armors its output. Use \code{\link[=age_encrypt_raw]{age_encrypt_raw()}} for unarmored binary output.}

\item{identities}{An \code{age_identity}, or character input accepted by
\code{\link[=age_identity]{age_identity()}}.}
}
\value{
\code{age_encrypt_text()} returns a length-1 character string;
\code{age_decrypt_text()} returns a length-1 UTF-8 string. \code{age_decrypt_text()}
signals \code{age_error_decrypt} if the plaintext is not valid UTF-8 text (for
binary payloads, use \code{\link[=age_decrypt_raw]{age_decrypt_raw()}}).
}
\description{
Convenience wrappers over \code{\link[=age_encrypt_raw]{age_encrypt_raw()}} / \code{\link[=age_decrypt_raw]{age_decrypt_raw()}} for one
string, ASCII-armored by default so the result is copy-pasteable. Encoding
is forced to UTF-8 on the way in and marked on the way out.
}
\examples{
id <- age_keygen()
rec <- age_pubkey(id)
ct <- age_encrypt_text("db_password_123", recipients = rec)
age_decrypt_text(ct, identities = id)
}
