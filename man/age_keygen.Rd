% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/identity.R
\name{age_keygen}
\alias{age_keygen}
\title{Generate a new age identity}
\usage{
age_keygen(path = NULL, overwrite = FALSE)
}
\arguments{
\item{path}{Optional file path. If given, the identity is written as a key
file (\verb{# created:} / \verb{# public key:} / \verb{AGE-SECRET-KEY-1...}) and the
object is returned invisibly. If \code{NULL} (default), nothing is written.}

\item{overwrite}{If \code{FALSE} (default) and \code{path} already exists, error
rather than clobbering an existing key.}
}
\value{
An \code{age_identity} object. Its print method shows only the public
key; the secret is never printed.
}
\description{
Creates a fresh X25519 identity (key pair). The secret key is generated in C
and never returned to R; to persist it, pass \code{path} so it is written
directly to a key file in the standard age format.
}
\examples{
id <- age_keygen()
id
age_pubkey(id)
}
