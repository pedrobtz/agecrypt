% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raw.R
\name{age_raw}
\alias{age_raw}
\alias{age_encrypt_raw}
\alias{age_decrypt_raw}
\title{Encrypt and decrypt raw vectors}
\usage{
age_encrypt_raw(x, recipients, armor = FALSE)

age_decrypt_raw(x, identities)
}
\arguments{
\item{x}{For \code{age_encrypt_raw()}, a raw vector of plaintext. For
\code{age_decrypt_raw()}, a raw vector of ciphertext, or a length-1 character
string of ASCII-armored ciphertext. Armor is auto-detected.}

\item{recipients}{Character vector of \code{"age1..."} recipient strings. Give
several to encrypt to multiple recipients; each can decrypt.}

\item{armor}{If \code{TRUE}, produce ASCII-armored (PEM) output. The return value
is still a raw vector (of the armored ASCII bytes).}

\item{identities}{An \code{age_identity}, or character input accepted by
\code{\link[=age_identity]{age_identity()}}. Tried in order; the first that matches wins.}
}
\value{
A raw vector: ciphertext for \code{age_encrypt_raw()}, plaintext for
\code{age_decrypt_raw()}.
}
\description{
The core buffer transforms: no file I/O, nothing written to disk. All other
verbs (\verb{_file}, \verb{_text}) build on these.
}
\examples{
id <- age_keygen()
rec <- age_pubkey(id)
ct <- age_encrypt_raw(charToRaw("hello"), recipients = rec)
rawToChar(age_decrypt_raw(ct, identities = id))
}
