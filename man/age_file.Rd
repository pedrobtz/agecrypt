% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/file.R
\name{age_file}
\alias{age_file}
\alias{age_encrypt_file}
\alias{age_decrypt_file}
\title{Encrypt and decrypt files}
\usage{
age_encrypt_file(
  input,
  output = NULL,
  recipients,
  armor = FALSE,
  overwrite = FALSE
)

age_decrypt_file(input, output = NULL, identities, overwrite = FALSE)
}
\arguments{
\item{input}{Path to the source file.}

\item{output}{Destination path. If \code{NULL} (default), \code{age_encrypt_file()}
appends \code{.age} to \code{input}; \code{age_decrypt_file()} strips a trailing \code{.age}.
If \code{input} does not end in \code{.age}, \code{age_decrypt_file()} requires \code{output}
to be given explicitly rather than guess.}

\item{recipients}{Character vector of \code{"age1..."} recipient strings.}

\item{armor}{If \code{TRUE}, produce ASCII-armored output.}

\item{overwrite}{If \code{FALSE} (default), error when \code{output} already exists.}

\item{identities}{An \code{age_identity}, or character input accepted by
\code{\link[=age_identity]{age_identity()}}.}
}
\value{
The output path, invisibly.
}
\description{
File-to-file encryption. Large files are streamed in constant memory in C
(they are not loaded into an R vector).
}
\examples{
id <- age_keygen()
rec <- age_pubkey(id)
f <- tempfile(fileext = ".txt")
writeLines("hello", f)
enc <- age_encrypt_file(f, recipients = rec)
dec <- age_decrypt_file(enc, output = tempfile(), identities = id)
readLines(dec)
}
